torch>=2.0
torchvision>=0.15
transformers>=4.30
# Pinned exactly, not a floor. dataset.py uses RandomResizedCrop with an area
# scale of (0.64, 1.44); values above 1.0 are accepted at 1.3.1 but rejected
# from 1.4 onward, so a newer release raises a validation error on import of
# the training dataset rather than reproducing the paper's augmentation.
albumentations==1.3.1
opencv-python>=4.7
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
networkx>=3.1
shapely>=2.0
scikit-image>=0.21
Pillow>=9.0
python-docx>=0.8
tqdm>=4.60
